// Family member roles. These strings are the contract with Firestore documents
// and security rules — do not change the values without a schema migration.
export const ROLE_PARENT = 'parent'
export const ROLE_CHILD = 'child'
